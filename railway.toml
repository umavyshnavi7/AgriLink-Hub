[build]
builder = "nixpacks"
buildCommand = "cd backend && mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar backend/target/agrilink-backend-1.0.0.jar"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
